<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>Login page</title>
</head>
<body class="bg-blue-400 min-h-screen flex flex-col">
    <!-- Navbar -->
    <nav class=" bg-gray-800 font-serif w-full shadow-md py-4 px-8 flex justify-between items-center ">
        <!-- Logo -->
        <img class="h-8 w-auto" src="watch-modified.jpg" alt="Company logo">
             <!-- Nav Links -->
        <div>
            <div class="hidden sm:ml-6 sm:block">
                <div class="flex space-x-4">
                    
                  <button class="font-serif font-bold rounded-md px-3 py-2 text-sm font-medium text-gray-300 hover:bg-gray-700 hover:text-white"
                       onclick="window.location.href='home.ejs';">
                       Home
                   </button>

                </div>
            </div>       
        </div>
    </nav>

    <!-- Main Content -->
    <div class="flex-grow flex items-center justify-center">
        <div class="w-full max-w-md bg-white rounded-2xl p-8 shadow-lg">
            <h3 class="text-center text-2xl mb-6 font-serif font-bold">Login</h3>
            <form action="/user/login" method="POST" class="space-y-6">
                <input id="email" type="email" 
                    name="email"
                    class="focus:ring-2 focus:ring-blue-500 focus:border-blue-500 border border-green-300 w-full px-4 py-2 text-lg bg-blue-50 rounded-md text-center font-serif" 
                    placeholder="Your Email" required>
                <input id="password" type="password" 
                    name="password"
                    class="focus:ring-2 focus:ring-blue-500 focus:border-blue-500 border border-green-300 w-full px-4 py-2 text-lg bg-blue-50 rounded-md text-center font-serif" 
                    placeholder="Password" required>
                <button type="submit" 
                    class="bg-blue-500 text-black px-4 py-1 rounded-lg shadow-lg hover:bg-blue-600 focus:outline-none focus:ring-4 focus:ring-blue-300 transition duration-150 active:scale-95 active:bg-blue-700 w-full text-lg font-bold rounded-md font-serif cursor-pointer">
                    Login
                </button>
            </form>
            <div class="mt-6 text-left">
                <button class="bg-blue-500 text-black font-bold px-4 py-1 rounded-lg shadow-lg hover:bg-blue-600 focus:outline-none focus:ring-4 focus:ring-blue-300 transition duration-150 active:scale-95 active:bg-blue-700 text-sm font-bold rounded-md font-serif cursor-pointer">
                    Forgot Your Password
                </button>
            </div>
            <div class="mt-8 text-left">
                <label class="text-sm font-serif font-bold">First time here?</label>
                <button class="bg-blue-500 text-black font-bold px-4 py-1 rounded-lg shadow-lg hover:bg-blue-600 focus:outline-none focus:ring-4 focus:ring-blue-300 transition duration-150 active:scale-95 active:bg-blue-700 text-sm font-bold rounded-md font-serif cursor-pointer"
                    onclick="window.location.href='signup.ejs';">
                    Create account
                </button>
            </div>
        </div>
    </div>
</body>
</html>